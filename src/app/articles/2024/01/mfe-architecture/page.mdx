import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
  author: 'Jake Hayes',
  date: '2024-01-10',
  title: 'Should I utilize a micro-frontend architecture?',
  description:
    "What is it? How do I use it? Should I even use it? Let's find out.",
  slug: '2024/01/mfe-architecture',
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

There are many different ways to structure and plan out any software that you build. Some ways are better than others and many times it is problem specific for which one is best.

One of those options has come to be known as micro-frontend architecture and that’s the one we are going to focus on.

## What is micro-frontend architecture?

Micro-frontend architecture

## What options are available to implement it?

## What are the benefits of implementing it?

## What are the downsides of implementing it?
